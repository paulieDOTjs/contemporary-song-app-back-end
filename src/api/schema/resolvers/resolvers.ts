import { merge } from "lodash";
import { GraphQLResolverMap } from "apollo-graphql";

//import all new resolvers made and merge them
// export const resolvers = (merge() as unknown) as GraphQLResolverMap;
